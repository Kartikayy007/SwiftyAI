# SwiftyAI

SwiftyAI is a Swift SDK for building AI features without turning your app into provider glue. The same application code can generate text, stream text, parse structured output, run tool loops, accept multimodal input, call media models, and manage SwiftUI state.

The SDK stays close to Swift. Models are protocols, responses are plain structs, streams are `AsyncThrowingStream`, and UI helpers are optional state objects rather than prebuilt screens.

## The SDK Shape

Most features start with a model and one function call:

```swift
import SwiftyAI

let model = OpenAICompatibleProvider(
    baseURL: "https://api.openai.com/v1",
    apiKey: ProcessInfo.processInfo.environment["OPENAI_API_KEY"]!,
    model: "gpt-4o-mini"
)

let response = try await generateText(
    model: model,
    prompt: "Explain Swift actors in three practical bullets."
)

print(response.text)
```

That same model can usually be reused for streaming, object generation, tools, and SwiftUI hooks if it conforms to the matching protocol.

## Core Features

- `generateText` returns a complete `AIResponse` with text, model, token usage, and finish reason when the provider sends them.
- `streamText` yields `AIStreamChunk` values as text arrives and can also call `onChunk` and `onFinish`.
- `GenerationOptions` carries system prompts, sampling controls, max tokens, stop sequences, per-request headers, retries, and prompt caching hints.
- `generateObject` and `streamObject` ask the model for JSON and validate it against an `AISchema`.
- `tool`, `dynamicTool`, `generateWithTools`, and `streamWithTools` run multi-step tool loops with stop conditions and callbacks.
- `AIChat` and `AICompletion` give SwiftUI apps observable state for chat and one-shot completions.

## Choosing A Path

Use direct provider instances when you want explicit dependencies and simple tests. Use model strings when your app has central configuration and wants calls like `"openai/gpt-4o-mini"` or `"ollama/llama3.2"`.

```swift
AI.configure { ai in
    ai.openAI(apiKey: ProcessInfo.processInfo.environment["OPENAI_API_KEY"]!)
    ai.ollama()
}

let cloud = try await generateText(
    model: "openai/gpt-4o-mini",
    prompt: "Write a one-line release note."
)

let local = try await generateText(
    model: "ollama/llama3.2",
    prompt: "Summarize this log file."
)
```

Call `AI.configure` during app startup before user-driven requests. For library code, tests, and dependency injection, prefer direct model instances or a local `ProviderRegistry`.

## Next Reads

Start with [installation](/docs/installation), then [provider setup](/docs/provider-setup) and [quickstart](/docs/quickstart). After that, read the feature pages for [text](/docs/generate-text), [streaming](/docs/stream-text), [objects](/docs/generate-object), [tools](/docs/tool-calling), and [SwiftUI hooks](/docs/swiftui-hooks).
